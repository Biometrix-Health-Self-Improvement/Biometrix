<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_home_screen_logged_in"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeScreen_Logged_In">

    <ScrollView
        android:id="@+id/HomeScreenLoggedInScrollView"
        style="@style/scrollview_parent_list"
        android:layout_below="@+id/dividerUpper">

        <TableLayout
            android:id="@+id/HomeScreenLoggedInTableLayout"
            style="@style/tablelayout_parent_list"
            android:layout_below="@+id/dividerupper">


        </TableLayout>
    </ScrollView>

</RelativeLayout>
